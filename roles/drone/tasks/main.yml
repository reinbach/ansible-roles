---
- include_tasks: install.yml
- include_tasks: config.yml

- include_role:
    name: traefik
  vars:
    docker_config: "yes"
    label: "{{ drone_label }}"
    domain: "{{ drone_domain }}"
    ip_address: "{{ ansible_default_ipv4.address }}"
