---
dependency:
  name: galaxy
  enabled: false
driver:
  name: podman
platforms:
  - name: instance-fathom
    image: docker.io/ubuntu:22.04
    privileged: true
provisioner:
  name: ansible
  log: true
verifier:
  name: ansible
lint: |
  set -e
  yamllint .
  ansible-lint .
  flake8
