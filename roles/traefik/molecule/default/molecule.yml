---
dependency:
  name: galaxy
  enabled: false
driver:
  name: podman
lint:
  name: yamllint
platforms:
  - name: traefik-instance
    image: ubuntu:18.04
    privileged: true
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
