---
dependency:
  name: galaxy
  enabled: false
driver:
  name: podman
platforms:
  - name: grafana-instance-grafana
    image: docker.io/ubuntu:20.04
    privileged: true
provisioner:
  name: ansible
  log: true
verifier:
  name: ansible
lint: |
  set -e
  yamllint .
  ansible-lint .
  flake8
