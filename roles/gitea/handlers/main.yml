---
- name: restart gitea
  service:
    name: gitea
    state: restarted

- name: set gitea auth
  command: "{{ gitea_bin | dirname }}/scripts/auth.sh"

- name: set gitea create_user
  command: "{{ gitea_bin | dirname }}/scripts/create_user.sh"
